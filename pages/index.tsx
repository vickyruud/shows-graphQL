import type { NextPage } from "next";
import Countries from "../components/Countries";

const Home: NextPage = () => {
  return (
    <div>
      <Countries />
    </div>
  );
};

export default Home;
